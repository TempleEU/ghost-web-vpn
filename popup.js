const $ = id => document.getElementById(id);
async function send(type, config) { return chrome.runtime.sendMessage(config === undefined ? { type } : { type, config }); }
function readForm() { return { scheme: $("scheme").value, host: $("host").value.trim(), port: Number($("port").value), username: $("username").value.trim(), password: $("password").value }; }
function render(c) { $("scheme").value = c.scheme || "socks5"; $("host").value = c.host || "127.0.0.1"; $("port").value = c.port || 1080; $("username").value = c.username || ""; const on = Boolean(c.enabled); $("toggle").setAttribute("aria-pressed", String(on)); $("toggle").classList.toggle("on", on); $("status").textContent = on ? "Connected" : "Disconnected"; }
async function load() { render(await send("getConfig")); }
$("toggle").addEventListener("click", async () => { const current = await send("getConfig"); if (current.enabled) { await send("disconnect"); $("message").textContent = "Proxy disconnected."; } else { const config = { ...readForm(), enabled: true }; if (!config.host || !Number.isInteger(config.port) || config.port < 1 || config.port > 65535 || (config.scheme === "socks5" && (!config.username || !config.password))) { $("message").textContent = "Enter a valid endpoint and proxy credentials."; return; } const result = await send("setConfig", config); $("message").textContent = result?.error || "Proxy connected."; } await load(); });
["scheme", "host", "port", "username", "password"].forEach(id => $(id).addEventListener("change", async () => { const current = await send("getConfig"); if (current.enabled) await send("setConfig", { ...readForm(), enabled: true }); }));
load().catch(e => { $("message").textContent = e.message; });
