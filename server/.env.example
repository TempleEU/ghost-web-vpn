# Ghost-controlled endpoint settings. Never commit a real .env file.
SERVERURL=vpn.example.com
SERVERPORT=51820
PEERS=ghost-web
PEERDNS=1.1.1.1
INTERNAL_SUBNET=10.66.66.0
PROXY_PORT=1080

# Required for the public SOCKS5 endpoint.
# Generate a unique strong value on the VPS; do not reuse passwords.
PROXY_USER=ghost-client
PROXY_PASSWORD=CHANGE_ME_TO_A_LONG_RANDOM_SECRET
